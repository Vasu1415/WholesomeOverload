<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cat Gallery</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Hedvig+Letters+Serif:opsz@12..24&family=Mooli&display=swap"
      rel="stylesheet"
    />
    <style>
        body {
            font-family: "Hedvig Letters Serif", serif;
            background-color:linear-gradient(to right, #0f2027, #000000);
            text-align: center;
            margin: 0;
            padding: 20px;
        }

        h1 {
            color: #ffb6c1;
            font-size: 4rem;
        }

        #cat-gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 10px;
            padding: 10px;
        }

        .cat-image {
            width: 100%;
            height: auto;
            border-radius: 8px;
            border: 2px solid #ccc;
            box-shadow: 2px 2px 5px #aaa;
        }
        .cat-image:hover{
            box-shadow: 4px 4px 10px crimson;
            transform: translate(-2px, -2px);
        }
        button {
            font-family: 'Hedvig Letters Serif', serif;
            color: white;
            padding: 10px 20px;
            background-color: black;
            border: 2px solid #ccc;
            border-radius: 5px;
            box-shadow: 2px 2px 5px #aaa;
            cursor: pointer;
            font-size: 2rem;
            transition: box-shadow 0.3s, transform 0.2s;
            color: #C8A2C8;
        }
        button:hover {
            box-shadow: 4px 4px 10px crimson;
            transform: translate(-2px, -2px);
        } 
        #buttons_info_entry{
            text-align: center;
            justify-content: center;
        }
    </style>
</head>
<body>
    <h1>Wholesome Cat Gallery</h1>
    <div id="cat-gallery">
        <% catImages.forEach(function(cat) { %>
            <img src="<%= cat.url %>" class="cat-image" alt="Cat">
        <% }); %>
    </div>
    <script>
        function getRandomColor() {
            const letters = '0123456789ABCDEF';
            let color = '#';
            for (let i = 0; i < 6; i++) {
                color += letters[Math.floor(Math.random() * 16)];
            }
            return color;
        }
        function setRandomBackgroundColor() {
            const body = document.body;
            const currentColor = window.getComputedStyle(body).backgroundColor;
            const newColor = getRandomColor();

            body.style.transition = 'background-color 1s';
            body.style.backgroundColor = newColor;

            setTimeout(() => {body.style.transition = '';}, 1000);
        }

        setInterval(setRandomBackgroundColor, 3000);        
        setRandomBackgroundColor();
        setTimeout(function() {
            window.location.reload();
        }, 10000); 
    </script>
    <button id = "buttons_info_entry" onclick="location.href='/'" value="Display My Creativity">Back to Home Page :O</button>
</body>
</html>